#include <iostream>
using namespace std;

int main() 
{
    const int tam = 5;
    int vet[tam];

    for(int i = 0; i < tam; ++i) 
    {
        vet[i] = i + 1;
    }

    for(int i = 0; i < tam; ++i) 
    {
        cout << "vet[" << i << "] = " << vet[i] << "\n";
    }

    return 0;
}
